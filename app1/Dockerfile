# set the base image
# this will bring everythin from httpd image like linux and the httpd server
FROM httpd

# copy your index.html to /usr/local/apache2/htdocs
# source: the file(s) from local machine
# destination: path in the image where the file(s) will be copied
COPY ./index.html /usr/local/apache2/htdocs/

